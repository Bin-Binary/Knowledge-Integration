# Python核心基础知识 — 问答题集

> Step 6 衍生品 | 用途: 自测知识掌握程度 | 覆盖所有12个概念层级

---

## L0: 对象模型 (6题)

**Q001** Python中"一切皆对象"具体意味着什么？一个Python对象在CPython中最基本的三个属性是什么？

**Q002** Python中变量赋值 `a = 1` 在底层做了什么操作？变量存储的是值还是引用？

**Q003** `is` 和 `==` 有什么区别？分别在底层用什么方式比较？

**Q004** 什么是可变对象(mutable)和不可变对象(immutable)？各举3个例子，并说明为什么这个区分如此重要。

**Q005** `id()` 函数返回什么？在CPython中它是什么？同一个值（如`256`）的两个变量id是否永远相同？

**Q006** Python是静态类型还是动态类型？强类型还是弱类型？分别举一个能说明Python类型系统的例子。

---

## L1: 基本数据类型 (8题)

**Q007** Python的int类型有范围限制吗？大整数运算`10**1000`会不会溢出？底层如何表示？

**Q008** `bool` 类型和 `int` 类型是什么关系？`True + 1` 的结果是多少？为什么？

**Q009** 浮点数 `0.1 + 0.2 == 0.3` 为什么是 `False`？这个现象是Python特有的吗？

**Q010** Python字符串内部是用什么编码存储的？PEP 393的灵活字符串表示解决了什么问题？

**Q011** `bytes` 和 `str` 有什么区别？`encode()` 和 `decode()` 分别在做什么操作？`b"hello"` 的类型是什么？

**Q012** `None` 是什么类型？有多少个`None`对象存在？为什么函数没有return时返回None？

**Q013** 切片操作 `s[start:stop:step]` 返回的是原对象的拷贝还是视图？对不可变类型和可变类型分别有何不同？

**Q014** `bytearray` 和 `bytes` 的区别在哪里？什么场景下你会选择`bytearray`而非`bytes`？

---

## L2: 复合数据类型 (7题)

**Q015** list底层是什么数据结构？在末尾`append()`的时间复杂度是多少？在中间`insert()`的时间复杂度是多少？为什么？

**Q016** tuple和list有什么本质区别？为什么tuple可以作为dict的key而list不行？空元组`()`是单例吗？

**Q017** Python 3.7+ 的dict为什么是"有序"的？这个顺序由什么决定？底层实现(PEP 412)是如何保持顺序的？

**Q018** dict的key为什么必须是可哈希(hashable)的？`hash()`函数用于什么？什么类型的对象不能被哈希？

**Q019** set和frozenset的本质区别是什么？为什么frozenset可以作为dict的key而set不行？

**Q020** 在遍历list/dict时添加或删除元素会发生什么？为什么？

**Q021** 自定义类如何让它支持 `len(obj)`、`obj[key]`、`for x in obj` 这些操作？需要实现哪些魔术方法？

---

## L3: 控制流 (5题)

**Q022** Python中的"真值测试"(Truth Value Testing)规则是什么？哪些值被认为是False？

**Q023** `for` 循环在底层是如何工作的？`for x in obj` 的执行流程是什么？

**Q024** `for/while` 循环的 `else` 子句什么时候执行？什么时候不执行？它与 `if` 的 `else` 有什么本质不同？

**Q025** `break` 和 `continue` 在嵌套循环中分别影响哪一层的循环？如何在深层循环中断出外层循环？

**Q026** `match/case` 语句（Python 3.10+）和 `if/elif/else` 链相比有什么优势？类匹配和守卫条件如何工作？

---

## L4: 函数与作用域 (9题)

**Q027** 函数定义 `def f(x=[])` 中的默认参数有什么陷阱？为什么？正确的做法是什么？

**Q028** Python的 *args 和 **kwargs 是什么？它们在函数内部分别是什么类型？如何用它们做函数转发？

**Q029** LEGB作用域规则是什么？当你在函数内部读取变量x时，Python按什么顺序寻找x？

**Q030** `global` 和 `nonlocal` 关键字各有什么作用？什么场景下必须使用？不使用会怎样？

**Q031** 什么是闭包(closure)？闭包在底层（CPython）是如何实现的？Cell对象是什么？

**Q032** 在循环中创建闭包（如`for i in range(3): funcs.append(lambda: i)`）会出现什么问题？为什么？如何修复？

**Q033** `lambda` 函数和 `def` 函数有什么本质区别？`lambda` 有什么限制？

**Q034** `map`/`filter`/`zip`/`enumerate` 分别返回什么？它们是立即求值还是惰性的？

**Q035** 什么是keyword-only参数（`*`后面的参数）和positional-only参数（`/`前面的参数）？各有什么用处？

---

## L5: 迭代与生成 (5题)

**Q036** 可迭代对象(iterable)和迭代器(iterator)有什么区别？为什么迭代器也是iterable？为什么说迭代器"只能消费一次"？

**Q037** `yield` 关键字做了什么？包含`yield`的函数和普通函数在被调用时有什么本质不同？

**Q038** `gen.send(value)` 是做什么的？为什么第一次必须 `send(None)` 或 `next(gen)`？

**Q039** `yield from` 解决了什么问题？它的底层工作机制是什么？

**Q040** 列表推导式 `[x**2 for x in range(10)]` 和生成器表达式 `(x**2 for x in range(10))` 的最大区别是什么？什么时候用哪个？

---

## L6: 面向对象 (9题)

**Q041** `class` 语句执行时发生了什么？`type(name, bases, namespace)` 这个底层API揭示了什么？

**Q042** `__new__` 和 `__init__` 有什么区别和调用顺序？什么场景需要重写`__new__`？

**Q043** 多继承时Python是如何解决"菱形继承"问题的？C3线性化算法的基本原则是什么？

**Q044** `super()` 的工作原理是什么？它如何确定下一步调用哪个父类？零参数 `super()` 依赖什么编译器魔法？

**Q045** 什么是描述符(descriptor)？`property`、`classmethod`、`staticmethod` 都是基于描述符实现的吗？

**Q046** 属性访问 `obj.attr` 的查找优先级是什么？数据描述符和非数据描述符有什么区别？

**Q047** `__slots__` 是什么？它解决了什么问题？使用`__slots__`有什么限制？

**Q048** 魔术方法 `__str__` vs `__repr__` 分别用在什么场景？`__eq__` vs `__hash__` 之间有什么隐式约定？

**Q049** `@classmethod` 和 `@staticmethod` 在调用时有什么不同？各自的典型使用场景是什么？

---

## L7: 模块与包 (6题)

**Q050** `import` 语句的完整执行流程是什么？Python如何避免重复加载同一个模块？

**Q051** `from X import Y` 和 `import X` 的区别是什么？各有什么优缺点？

**Q052** `sys.path` 是什么？模块搜索顺序是怎样的？如何在程序中动态添加搜索路径？

**Q053** `__all__` 有什么作用？`from module import *` 如果模块没有定义 `__all__` 会导入什么？

**Q054** `__name__ == "__main__"` 这个条件有什么作用？为什么几乎所有Python脚本都在底部写这个判断？

**Q055** 什么是循环导入？为什么会导致部分属性不可见？如何避免循环导入？

---

## L8: 异常处理 (6题)

**Q056** Python异常层次结构的根是什么？`BaseException` 和 `Exception` 有什么区别？为什么不建议 `except Exception` 捕获一切？

**Q057** `try/except/else/finally` 的执行流程是怎样的？在什么情况下`else`执行、`finally`不执行？

**Q058** `finally` 中的 `return` 或 `break` 会发生什么？它对异常传播有什么影响？

**Q059** `raise X from Y` 是什么作用？`X.__cause__` 和 `X.__context__` 分别记录什么？两者有何不同？

**Q060** `with` 语句在底层等价于什么结构？如果`__enter__`本身抛异常，`__exit__`会被调用吗？

**Q061** 如何定义自定义异常类？为什么推荐继承`Exception`而非`BaseException`？

---

## L9: 文件与I/O (4题)

**Q062** `open()` 函数的文本模式(`'r'`)和二进制模式(`'rb'`)有什么区别？在二进制模式下读取的是str还是bytes？

**Q063** 文件对象的 `read()`、`readline()`、`readlines()` 有什么区别？如何惰性地逐行读取一个大文件？

**Q064** `print()` 的输出目标可以改变吗？如何将`print()`的输出重定向到文件或字符串？

**Q065** `sys.stdout` 和 `sys.stderr` 有什么不同？什么场景需要写入stderr而非stdout？缓冲策略有什么差异？

---

## L10: 高级特性 (5题)

**Q066** `@decorator` 语法糖等价于什么操作？装饰器在什么时候执行——定义时还是每次调用时？

**Q067** 如何编写一个带参数的装饰器（装饰器工厂）？内层外层分别什么时候执行？

**Q068** 为什么在写装饰器时推荐使用 `@functools.wraps(func)`？不用的后果是什么？

**Q069** 函数注解 `def f(x: int) -> str:` 中的类型标注在运行时会被强制检查吗？`__annotations__` 字典存了什么？

**Q070** f-string `f"{expr}"` 和 `.format()` 相比有什么优势？f-string支持哪些格式说明符？`f"{x=}"` 是什么？

---

## L11: 内存与执行模型 (6题)

**Q071** CPython的引用计数(Reference Counting)是如何工作的？什么情况下引用计数会失败导致内存泄漏？

**Q072** Python的分代垃圾收集(GC)解决了什么问题？三代(gen0/gen1/gen2)有什么区别？GC什么时候触发？

**Q073** 浅拷贝(copy.copy)和深拷贝(copy.deepcopy)的区别是什么？不可变对象做深拷贝会发生什么？

**Q074** GIL(全局解释器锁)是什么？它对多线程编程有什么影响？什么场景下多线程有效、什么场景下无效？

**Q075** .pyc文件是什么？存储在什么位置？什么时候会重新生成？.pyc文件可以跨Python版本使用吗？

**Q076** Python源码的执行流程（源码→字节码→执行）是怎样的？如何用 `dis` 模块查看某个函数的字节码？

---

## 总计: 76题, 覆盖全部12个层级、86个核心概念

| 层级 | 题数 | 编号范围 |
|:---|:---|:---|
| L0 对象模型 | 6 | Q001-Q006 |
| L1 基本类型 | 8 | Q007-Q014 |
| L2 复合类型 | 7 | Q015-Q021 |
| L3 控制流 | 5 | Q022-Q026 |
| L4 函数作用域 | 9 | Q027-Q035 |
| L5 迭代生成 | 5 | Q036-Q040 |
| L6 面向对象 | 9 | Q041-Q049 |
| L7 模块包 | 6 | Q050-Q055 |
| L8 异常处理 | 6 | Q056-Q061 |
| L9 文件I/O | 4 | Q062-Q065 |
| L10 高级特性 | 5 | Q066-Q070 |
| L11 内存执行 | 6 | Q071-Q076 |
| **合计** | **76** | |
