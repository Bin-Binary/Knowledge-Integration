---
name: generate-frame
description: >
  FrameNet 端到端框架语义学分析技能。对给定主题、语料（文本/地址）或命令执行完整建模流水线：
  获取语料 → 原文翻译 → 事实/缺口/误解抽取 → 主题入口框架 + 语义框架表 → 框架间关系表
  （FrameNet 标准 9 种关系）。输出文件族：主文档 .md 始终生成；
  可选项：① 缺口补全（独立文件，从官方/权威源检索）；② HTML 统一视图页面。
  均为同级文件、由主文档引用。当用户要求"框架分析 / FrameNet 标注 / 语义框架建模 /
  知识结构审计 / 补全缺口 / 生成html页面"，或显式调用 generate-frame 时使用本技能。
---

# generate-frame — FrameNet 端到端框架语义学分析

## 1. 技能定位

将 FrameNet 框架语义学（Fillmore 理论传统；理论手册：*FrameNet II: Extended Theory and Practice*, Ruppenhofer et al., 2016）落地为可执行的端到端标注流水线。

**核心模型**：
- **Target**：框架触发词，激活一个独立完整事件；
- **Frame**：脚本式概念结构，描述某类情境/对象/事件及其参与者与道具；
- **FE（Frame Element）**：框架元素 = 抽象语义槽（如 Apply_heat 框架的 Cook / Food / Heating_instrument）。

**一句话概括**：把任意领域语料转化为「入口框架 → 子框架 → 框架元素 → 框架关系」的语义结构，并同步产出**事实、缺口、误解**三类知识审计清单；**全部中间产物必须留存，禁止丢弃**。

**方法边界声明**（自知之明）：本方法产出的是"FrameNet 风格的主题知识结构化"，非严格 FrameNet 标注——框架由主题维度而非 LU 激活；LU 层（Target↔Frame 映射）暂不持久化，属已知局限。

## 2. 输入识别与预处理

| 输入类型 | 识别特征 | 预处理动作 |
|---|---|---|
| 主题 | 名词性领域词，如"Python异常体系"、"HTTP状态码" | 搜集该领域**权威原文**（官方文档优先），作为分析语料 |
| 语料地址 | URL / 文件路径 / 文档引用 | 读取原文，按片段顺序编号切分 |
| 命令 | 指令式表述，如"分析X的框架结构" | 解析出目标分析对象，归入上两类处理 |

> 语料选取原则：权威、可溯源（官方文档 > 教材 > 高质量技术文档）；每个片段必须可编号定位。

## 3. 端到端工作流（总控 + 2 可选步）

| 步骤 | 动作 | 产出 |
|---|---|---|
| 1 | 获取原始语料（按第2节识别输入类型） | 编号片段集合 |
| 2 | 生成【原文+翻译】：一一对应，原文后紧跟中文翻译，记录来源与片段位置 | 原文和翻译章节 |
| 3 | 抽取【事实】：客观可验证陈述，作为 FE 填充值 | 事实清单 |
| 4 | 识别【缺口】：语料未说明、定义缺失、边界模糊、NI 信息空白 | 缺口清单 |
| 5 | 推导【误解】：学习者可能的错误推论，每条须可回溯原文 | 误解清单 |
| 6a | 识别**子框架**：框架名称、刻画描述、抽象 FE（含核心度标注）、事实填充 | 子框架定义集 |
| 6b | 构建**主题入口框架**（见 5.6）：维度槽设计 + 完整性校验 | 入口框架 |
| 7 | 构建框架表格（五列固定，**无 Gap 列**；入口框架固定为第一行） | 框架表 |
| 8 | 标注框架间关系（仅 FrameNet 标准 9 种，见 5.5）：第一组 = 子框架→入口框架；第二组 = 子框架之间 | 关系表 |
| 9 | 输出主文档（7 节固定，见 8.1） | `{主题}-framenet.md` |
| 10（可选） | 缺口补全（见 7.1）：产出**同级独立文件**，主文档以引用行链接 | `{主题}-gapfill.md` |
| 11（可选） | HTML 统一视图（见 7.2）：合并渲染主文档 + gapfill | `{主题}-framenet.html` |

> 步骤 3–6a 的句子级推导过程，遵循第 4 节「微观七步法」；步骤 10–11 为可选项，默认关闭。

## 4. 微观七步法（句子级语料分析）

> 输入：一段原始自然语料段落。
> 前置规则：系动词 be 为 Cop 系动词，**不计入 Target**。
> 流程：①识别 Target → ②匹配 Frame → ③定位 FE → ④FE-GF-PT 三元组（仅表层句法依存）→ ⑤零实例化 NI + 特殊标注检查 → ⑥框架-框架关系 → ⑦业务洞察

### 4.1 识别 Target
- 逐句提取**可激活独立完整事件的谓词**；
- 每一个从句内的完整语义事件，独立作为一个 Target；
- 动词、形容词谓词均可成为 Target；
- 主动/被动形式的同语义谓词，各自独立 Target。

### 4.2 匹配 Frame
- 按每个 Target 的义项匹配 FrameNet 标准框架；
- 区分【核心 FE】（框架必需参与者）与【外围 FE】（可选补充参与者）；
- 记录每个 FE 的语义角色含义。

### 4.3 定位 FE 文本片段
- 在当前 Target 的**句法辖域内**找到每个 FE 对应的表层文本；
- 若 FE 概念存在但**无表层文本片段**：记录该状态，进入 4.5 零实例化判断。

### 4.4 FE-GF-PT 三元组表格（仅表层句法实现的 FE）
> 只填入拥有表层句法填充物的 FE；零实例化 FE 不进入本表。

| 字段 | 说明 |
|---|---|
| 文本片段 | 表层文本 |
| FE | 语义角色 |
| GF | Ext 外部论元 / Dep 依存论元 / Obj 宾语 / PP 介词短语 / Sfin 限定从句 |
| PT | 归属 Target-Frame 标记（格式：`词元 → 框架名`） |
| 归属Target | Target 词元（含词性，如 `bake.v`） |

### 4.5 零实例化 NI 判断 + 特殊标注检查
1. 零实例化三型（标记类型并写出语义解释）：
   - **INI**（不定零实例化）：FE 概念存在、无表层文本，指代集合内泛指实体；
   - **DNI**（定指零实例化）：无表层文本，但文中存在可恢复的先行语；
   - **CNI**（构式零实例化）：被构式（被动/中动/祈使）压制掉表层实现，概念上仍存在。
2. 特殊标注检查，逐一记录：FE 合并、非连续框架元素、Supp 支持谓词、Ctrlr 控制器。

### 4.6 框架-框架语义关系
- 判断多个 Target 之间的语义依赖；
- 严格使用 FrameNet 标准 9 种关系（全集与判定级联见 5.5）；
- 输出关系表：源框架｜FrameNet关系｜目标框架｜关系释义。

### 4.7 业务洞察（**严格只使用当前段落原文，禁止引入外部领域知识**）
- ✅ 确定事实：文本字面直接提取的客观断言；
- ⚠️ 信息缺口：本段原文未交代、缺少定义、边界模糊、INI 带来的信息空白；
- ❗ 易误解：基于文本表述，学习者容易脑补的错误推论，**每条须回溯原文文本**。

## 5. FrameNet 理论决策要点

### 5.1 框架切分清单（同框架词汇必须满足，按可操作性排序）
1. **相同 FE**：数量与类型一致（含显式与 NI 实现）；
2. **指称场景相同部分**；
3. **相同 FE profiling**：同一参与者视角被凸显（如 buy ≠ sell，须分帧）；
4. **相同关系**：FE 间互指关系一致、对背景框架的预设一致；
5. **类型相似**：Target 基本指称相似、FE 类型预指相似。

附加校验——**体一致性**：同框架 LU 必须蕴含相同的阶段与过渡；causative 与 inchoative 因多出一个致使子事件，须分帧（分帧后用 `Causative of` / `Inchoative of` 关联回来）。

> 适用性说明：本清单为"词归帧"判据；本方法的子框架属"事实归帧"，内聚性以入口框架维度槽（5.6）做粗粒度校验替代。

### 5.2 不作为切分依据的标准
- **语法差异**：被动、中动、时体构式、与外部主题 FE 的组合、纯词性差异（用 CNI 处理，不分帧）；
- **标量/极性反义**：反义词同框架；但**换位 converse**（buy/sell）与**逆反对**（tie/untie）须分帧；
- **用法差异**：指示、语域、方言、评价——用语义类型标记，不分帧。

### 5.3 FE 定义规则
- FE 是**抽象语义槽**，以语义角色命名，**禁止直接把事实文本当作 FE**；
- 事实仅作为 FE 的**填充值**进入框架表；
- 核心度三级：Core / Peripheral / Extra-Thematic（Duration、Frequency、Recipient、Beneficiary、Explanation、Condition、Concessive、Degree 等）；
- **核心度标注（v4 起，输出强制）**：每个 FE 在框架表中标注（核心）或（外围）。判定法：删除该槽后框架刻画是否仍完整——不完整→（核心）；仅补充语境/列举→（外围）；
- 同一语义槽的不同填充物本体类型不同时，拆分为两个 FE 并用 Excludes 关联。

### 5.4 GF / PT 速查（理论背景）
- 动词 GF：Ext / Obj / Dep；名词 GF 另有 Gen / Quant / Appos；
- FrameNet PT 全集：NP、N、Poss、PP、VPfin、Sfin、Sinterrog、Swhether、Sing、Srel、Sto、Sforto、Sbrst、Sub、AJP、A、AVP、QUANT、QUO；
- 本流程表格规格以 4.4 节简化集合为准。

### 5.5 框架间关系（FrameNet 标准全集，依据手册 §6.1）

输出**仅允许**以下 9 种标准关系标签，禁止自定义：

| 关系标签 | 中文名 | 定义 | 判定要点 |
|---|---|---|---|
| `Inheritance` | 继承 | 子框架 IS-A 父框架；父框架每个 FE 绑定到子框架对应 FE，子框架可新增扩展语义 | 源是目标的本体论子类型，且父 FE 可全部对应（显式对应或经继承链隐式对应） |
| `Perspective on` | 视角化 | 子框架对未视角化的父框架提供特定参与方视角 | 目标为未视角化背景场景，源凸显某一方视角 |
| `Using` | 使用 | 子框架预设父框架为语义背景；父 FE 无需全部绑定 | 源语义成立必须以目标为前提，但源**不是**目标的子类型；**释义必须点名预设的具体前提** |
| `Subframe` | 子框架 | 子框架是父框架所表复杂事件的一个子事件/阶段 | 目标为分阶段复杂事件，源为其一阶段 |
| `Precedes` | 先行 | 源框架事件在目标框架事件之前发生 | 源与目标通常为同一复杂事件的子框架，源先于目标 |
| `Causative of` | 致使 | 子框架是父框架的致使对应框架 | 源 = 目标场景 + 外部致事 |
| `Inchoative of` | 起始 | 子框架是父框架（状态）的起始对应框架 | 源刻画进入目标状态的起始变化，不词汇化致事 |
| `Metaphor` | 隐喻 | 子框架是父框架（具体源域）的隐喻映射 | 跨域映射 |
| `See also` | 参见 | 弱关联链接，无形式化语义 | 不满足以上任何强判定，仅存在领域相关性；兜底慎用 |

**方向约定**：源框架为子/依赖方，目标框架为父/背景方；`Precedes` 为时序方向（源先于目标）。

**标签选择级联**（按序检验，首个命中即用）：
1. 子类型 + 父 FE 可全部对应 → `Inheritance`
2. 不满足子类型，但源语义成立必须以目标为前提 → `Using`
3. 源是目标复杂事件的阶段 → `Subframe`；同父先后阶段 → `Precedes`
4. 状态-变化对：源 = 目标 + 致事 → `Causative of`；源 = 目标状态之起始 → `Inchoative of`
5. 目标未视角化、源凸显一方视角 → `Perspective on`
6. 跨域映射 → `Metaphor`
7. 仅领域相关 → `See also`

**质量红线**：每条关系须通过对应判定要点检验；无合格框架对的关系类型**宁缺毋滥**。

**范围界定**：本节仅规定框架间关系；FE 间关系（`Requires`、`Excludes` 等）属于框架内部标注，不进入框架关系表。

**旧标签迁移**（v1 → v2+）：`Is a Punctualization of` → 逐条复检为 `Inheritance` 或 `Using`；`Parallels` → `See also`。

### 5.6 主题入口框架（v4 新增）

**定位**：主题的**根锚点 / 背景场景框架**——对"该主题由哪些维度构成"的元级刻画。对应 FrameNet 中背景场景帧之于子帧的角色（如 Commercial_transaction 之于 Commerce_buy / Commerce_sell）。职能：聚合、导航、**完整性校验**。

**命名**：`{主题核心词}入口框架`（如"异常体系入口框架"、"闭包入口框架"）。

**FE 设计**：
- `主题对象`（核心）：主题本体，填充为主题指称；
- 每个构成维度一个**维度槽**（通常核心；边界标记类维度可标外围），填充 = **维度摘要（须可回溯事实清单）+ 对应子框架名**。

**填充特例（对"填充必须为事实"的唯一显式例外）**：入口框架维度槽的填充值允许包含子框架名引用（导航指针）。理由：入口框架是网络根节点，其职能是聚合导航；维度摘要部分仍须来自事实清单。

**完整性校验（质量门）**：
- 每个子框架**必须**填充入口框架的至少一个维度槽；
- 出现"无槽可填的子框架" → 或者入口框架缺维度，或者该框架不属于本主题——二者必居其一，必须修正；
- 反向：入口框架的每个维度槽必须有子框架填充（空维度槽 = 虚设维度，删除或补框架）。

**表格位置**：框架表**第一行**，固定。

**关系约定**：关系表**第一组** = 各子框架 → 入口框架，每行释义注明填充的维度槽；标签按 5.5 级联判定（静态主题多为 `Using`；过程型主题可为 `Subframe`，阶段间用 `Precedes`）。

## 6. 三类中间产物判定标准

| 概念 | 定义 | 判定问题 |
|---|---|---|
| **事实** | 原文直接陈述的客观信息 | "原文是否直接说了？" |
| **缺口** | 语料本身缺少、未说明的知识 | "原文是否没交代/定义缺失/边界模糊？" |
| **误解** | 学习者由文本诱导产生的错误理解 | "读者会不会由这段话推出错误结论？" |

**红线**：三者不可混写；每条误解须注明诱导它的原文表述；缺口不得用外部知识脑补填补（启用可选项①时通过独立文件补全，见 7.1）。

## 7. 可选项配置与输出文件族

| 选项 | 触发方式 | 产出文件 |
|---|---|---|
| **缺口补全**（fill-gaps） | "补全缺口 / 查权威资料 / 填补空白 / gap-fill" | `{主题}-gapfill.md`（同级独立文件） |
| **HTML 页面**（html） | "生成 html / 网页 / 可视化页面" | `{主题}-framenet.html`（同级独立文件） |

默认关闭；两项可独立或同时启用；启用状态写入主文档「说明」章节。

### 7.0 输出文件族（同级目录）

```
{主题}-framenet.md        主文档（始终生成；不依赖外部文件即完整成立）
{主题}-gapfill.md         缺口扩展资料（可选项①启用时生成）
{主题}-framenet.html      统一视图页面（可选项②启用时生成）
```

**主文档引用规则（"如果有的话"）**：引用行**仅**在对应文件实际生成时出现，使用同级相对路径：
- 启用① → `##缺口` 章节末尾：`> 缺口扩展资料（可选项①）：见 [{主题}-gapfill.md](./{主题}-gapfill.md)`
- 启用② → `##说明` 相应条目内给出 `[{主题}-framenet.html](./{主题}-framenet.html)` 链接

**主文档独立性**：缺口清单本身保留在主文档内，仅**补全资料**外置——两个附属文件缺失时主文档仍语义完整。

### 7.1 缺口补全规范（产出：`{主题}-gapfill.md`）
- **来源白名单**（按优先级）：官方文档 > 官方 PEP/RFC/W3C 规范 > 官方语言参考/库参考 > 官方教程 > 领域权威标准（如 MDN）。**禁止**博客、论坛、问答站、第三方教程；
- 每条补全记录四要素：**缺口编号、补全资料、权威来源名称与具体定位（章节/条目/PEP 编号）、补全状态**；
- 补全状态三档：✅已补全 / ◐部分补全 / ❌未找到权威资料；
- **红线1**：补全资料不修改主文档事实清单、不进入框架表 FE 填充——v4 起物理隔离（独立文件），隔离更强；
- **红线2**：未找到权威资料如实标 ❌，禁止推测或非权威源填充；
- **红线3**：补全资料与原文冲突时单独标注"⚠️冲突提示"，不擅自改写原文事实；
- **文件结构**：标题 → 被引用说明（回链主文档）→ 来源与红线声明 → 补全表（四列：缺口编号｜补全资料｜权威来源与定位｜补全状态）→ 冲突提示区（如有）。

### 7.2 HTML 统一视图规范（产出：`{主题}-framenet.html`）
- **定位**：主文档 + gapfill 的**合并渲染视图**（md 文件族是模块化的，html 是统一视图；自包含单文件约束不变）；
- **自包含单文件**：CSS 全部内联，无外部依赖、无外链脚本，离线双击可开；
- **结构**：页头（标题 + 版本徽章 + 启用选项徽章 + 文件族说明）→ 粘性目录导航（章节锚点）→ 各章节卡片；
- **入口框架**：框架表第一行以高亮样式（如 `.entry`）区分，导航中标注；
- **表格**：框架表、关系表（分两组呈现）、补全表完整渲染，外层横向滚动容器；
- **关系标签**渲染为彩色徽章，9 种标准关系各一色，附完整图例；
- **清单配色**：事实（绿）/ 缺口（橙）/ 误解（红）左侧色条；补全状态徽章复用 7.1 三档标记；
- 原文片段用 `<details open>` 折叠组件承载；响应式窄屏自适应。

## 8. 输出文档规范

### 8.1 主文档章节固定顺序（7 节）
```markdown
# {主题} FrameNet 语义框架标注文档
> 文档结构：##说明 → ##原文和翻译 → ##事实 → ##缺口 → ##误解 → ##{主题}框架 → ##{主题}框架间关系
```
> 末两章节名以分析主题命名；缺口补全**不再内联**（可选项①启用时为同级文件 + 缺口章节末尾引用行）。

### 8.2 框架表格（五列固定，无 Gap 列）
| 框架名称 | 刻画描述 | 框架元素（FE）及填充 | 原始事实语料 | 原始语料定位 |
- **第一行固定为入口框架**（5.6）；
- FE 及填充格式：`` `{FE名}`（核心/外围）: 填充值 ``，多个 FE 用 `<br>` 分隔；
- 原始事实语料：编号列出支撑该框架的事实；原始语料定位：片段编号区间 + 参考链接。

### 8.3 框架关系表格（四列固定，两组呈现）
| 源框架 | FrameNet关系 | 目标框架 | 关系释义 |
- **第一组**：各子框架 → 入口框架（释义注明填充的维度槽）；
- **第二组**：子框架之间的关系；
- 表格前附 9 种标准标签图例；关系释义须含判定依据（如 FE 对应证据、预设的具体前提）。

### 8.4 完整骨架模板

```markdown
# {主题} FrameNet 语义框架标注文档
> 文档结构：##说明 → ##原文和翻译 → ##事实 → ##缺口 → ##误解 → ##{主题}框架 → ##{主题}框架间关系

## 说明
1. 本文件基于{语料来源说明}，采用**FrameNet框架语义学**进行建模。
2. 框架元素FE为抽象语义槽；**事实为槽填充客观内容；缺口是从原始语料推导出来的知识缺失点；误解是基于语料推导的学习者可能产生的错误理解**。
3. 原文与翻译一一对应，原文后紧跟中文翻译；
4. 框架关系使用FrameNet标准9种关系标签：`Inheritance`、`Perspective on`、`Using`、`Subframe`、`Precedes`、`Causative of`、`Inchoative of`、`Metaphor`、`See also`；
5. 原始语料定位记录原文出处、片段位置与参考链接。
6. （启用选项时）本次分析启用可选项：**①缺口补全**——独立文件 [{主题}-gapfill.md](./{主题}-gapfill.md)；**②HTML统一视图**——[{主题}-framenet.html](./{主题}-framenet.html)。
7. 问答档案（qa-5w2h 技能产出）：见 {主题}-qa.md

## 原文和翻译
> 来源：{权威来源名称与链接}
1.
> Original: {原文片段1}
> 翻译：{中文翻译1}
（……逐片段编号）

## 事实
> 全部从{来源}提取的客观事实，作为框架FE的填充值，属于分析中间产物
1. {客观可验证陈述}（←片段n）
（……）

## 缺口
> 从原始语料分析得出的**知识缺口**：语料没有明确说明、缺少定义、边界模糊的点，属于分析中间产物
1. {知识空白点}（相关片段n）
（……）
> （启用①时）缺口扩展资料（可选项①）：见 [{主题}-gapfill.md](./{主题}-gapfill.md)

## 误解
> 从原始语料推导的学习者**潜在错误理解**，属于分析中间产物
1. {错误推论}（←片段n诱导）
（……）

## {主题}框架
|框架名称|刻画描述|框架元素（FE）及填充|原始事实语料|原始语料定位|
|---|---|---|---|---|
|{主题核心词}入口框架|主题级根锚点：{聚合与导航职能描述}|`主题对象`（核心）: {主题指称}<br>`{维度1}维度`（核心）: {维度摘要}——{子框架1}<br>`{维度k}维度`（外围）: {摘要}——{子框架k}|1. {支撑维度划分的事实}|原文片段{全区间}；参考链接：{链接}|
|{子框架名}|{刻画描述}|`{FE名}`（核心）: {填充}<br>`{FE名}`（外围）: {填充}|1. {事实}|原文片段{n}‑{m}；参考链接：{链接}|
（……入口框架固定第一行）

## {主题}框架间关系
> FrameNet标准关系：`Inheritance`（继承）、`Perspective on`（视角化）、`Using`（使用/依赖）、`Subframe`（子框架）、`Precedes`（先行）、`Causative of`（致使）、`Inchoative of`（起始）、`Metaphor`（隐喻）、`See also`（参见）

**第一组：子框架 → 入口框架**
|源框架|FrameNet关系|目标框架|关系释义|
|---|---|---|---|
|{子框架}|{标签}|{主题核心词}入口框架|填充`{维度}维度`槽；{预设的具体前提}|
（……每个子框架一行）

**第二组：子框架之间**
|源框架|FrameNet关系|目标框架|关系释义|
|---|---|---|---|
|{源框架}|{标签}|{目标框架}|{判定依据}|
（……未使用的关系类型附"宁缺毋滥"说明）
```

## 9. 硬性约束（违反即返工）
1. FE 必须是抽象语义角色，禁止直接把事实文本当作 FE；
2. 只能使用 FrameNet 标准 9 种关系标签（见 5.5），禁止自定义关系；每条关系须通过判定要点检验，禁止硬凑；Using 的释义必须点名预设的具体前提；
3. 事实、缺口、误解是分析中间产物，必须完整记录，不能丢弃；
4. 严格区分三个概念：事实 = 原文直接陈述；缺口 = 语料本身缺少；误解 = 文本诱导的错误理解；
5. 原始语料定位需记录片段编号、来源链接；
6. 主文档输出标准 Markdown（7 节固定），不依赖附属文件即完整成立；
7. 框架表不含 Gap 列；第一行固定为入口框架；
8. 原文与翻译一一对应，原文后紧跟翻译；
9. 事实/缺口/误解清单中每条内容须可回溯到编号原文片段；
10. 缺口补全仅可使用官方/权威来源并逐条记录定位；未找到如实标 ❌；补全资料不得混入主文档事实清单或框架表 FE 填充，冲突条目以 ⚠️冲突提示 单独标注；
11. 每个 FE 必须标注核心度（核心/外围），判定依据 5.3；
12. 入口框架完整性：每个子框架必须填充至少一个维度槽，每个维度槽必须有子框架填充（5.6 质量门）；
13. 引用行仅在实际生成对应文件时出现，使用同级相对路径；
14. HTML 为统一视图（主文档 + gapfill 合并渲染），自包含单文件，内容与 md 文件族一致。

## 10. 交付前自检清单
- [ ] 章节齐全且顺序正确（主文档 7 节）
- [ ] 框架表第一行为入口框架；维度槽 ↔ 子框架双向无缺漏（无孤儿框架、无空槽）
- [ ] 每个框架的 FE 全部为抽象语义槽且带核心度标注
- [ ] 每个 FE 填充值可对应到事实清单条目（入口框架维度摘要同样可回溯）
- [ ] 每条误解可回溯到具体原文表述
- [ ] 缺口未被外部知识"补齐"（补全仅存在于独立文件）
- [ ] 关系表两组齐全；只用了 9 种标准标签，判定与 5.5 级联一致，未硬凑
- [ ] 表格字段顺序与规定一致，框架表无 Gap 列
- [ ] 语料定位含片段编号与参考链接
- [ ] （启用①时）`{主题}-gapfill.md` 为独立文件、每条缺口有补全状态、被主文档 ##缺口 引用行链接
- [ ] （启用②时）`{主题}-framenet.html` 合并渲染主文档+gapfill、单文件可离线打开、入口框架行高亮、被主文档 ##说明 链接
- [ ] 文件族保存后可正常渲染/打开

## 11. 附属文件与黄金范例
- `references/examples`: 完整实例目录；
- `references/book.pdf`：FrameNet II 手册，框架开发、FE 核心度、GF/PT、零实例化、框架关系全集的理论依据；
- 框架命名范式：子框架"{对象}{机能}框架"；入口框架"{主题核心词}入口框架"；
- 已知局限（留档）：LU 层不持久化；"事实归帧"内聚判据以维度槽粗粒度替代；
- 可选项使用示例："generate-frame: 分析X"（基线）/"分析X，补全缺口并生成html"（双选项启用，产出三文件族）。

---

