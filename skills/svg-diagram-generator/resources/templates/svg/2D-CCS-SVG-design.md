# 二维直角坐标系全局视图SVG设计说明
> 用于SVG绘制全局视图场景视图时使用

## 概述
选取二维直角坐标系来进行全局观察，x y轴代表不同视角, 坐标含义按xy具体含义判断

## 结构 & 生命周期
> 从虚拟内存分区和硬件映射的关系来观察对象
x轴代表生命周期、y轴以虚拟内存分区为主标签（代码段/堆/栈），附注硬件映射路径（SSD/DRAM/Cache/CPU）、坐标描述："谁在某虚拟分区（某硬件）上干了某事"
> 核心规则：y轴主标签使用虚拟分区体现内存管理语义；每个数据点和图例必须标注硬件映射路径。虚拟分区不能停留在OS抽象层，必须落实到具体硬件组件。
